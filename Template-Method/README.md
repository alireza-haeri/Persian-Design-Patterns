# الگوی Template Method (متد الگو)

## 🎯 هدف
الگوی Template Method یک الگوی طراحی رفتاری است که اسکلت یک الگوریتم را در کلاس پایه تعریف می‌کند اما به زیرکلاس‌ها اجازه می‌دهد مراحل خاصی از الگوریتم را بدون تغییر ساختار آن بازنویسی کنند.

## 💻 مثال کد (Python)

```python
from abc import ABC, abstractmethod

class Beverage(ABC):
    def prepare(self):
        """Template Method"""
        self.boil_water()
        self.brew()
        self.pour_in_cup()
        self.add_condiments()
        print("✅ نوشیدنی آماده است!\n")
    
    def boil_water(self):
        print("💧 جوشاندن آب")
    
    def pour_in_cup(self):
        print("☕ ریختن در فنجان")
    
    @abstractmethod
    def brew(self):
        pass
    
    @abstractmethod
    def add_condiments(self):
        pass

class Tea(Beverage):
    def brew(self):
        print("🍵 دم کردن چای")
    
    def add_condiments(self):
        print("🍋 اضافه کردن لیمو")

class Coffee(Beverage):
    def brew(self):
        print("☕ دم کردن قهوه")
    
    def add_condiments(self):
        print("🥛 اضافه کردن شیر و شکر")

# استفاده
print("آماده‌سازی چای:")
tea = Tea()
tea.prepare()

print("آماده‌سازی قهوه:")
coffee = Coffee()
coffee.prepare()
```

## 🔍 چه زمانی استفاده کنیم؟

1. زمانی که می‌خواهید کلاینت‌ها فقط مراحل خاصی از الگوریتم را گسترش دهند
2. زمانی که کلاس‌های متعددی دارید که الگوریتم‌های مشابهی دارند
3. برای کنترل نقاط گسترش

---

> **یادآوری**: Template Method ساختار الگوریتم را حفظ می‌کند اما جزئیات را قابل تغییر می‌سازد! 📋
