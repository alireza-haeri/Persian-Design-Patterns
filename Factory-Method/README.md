# الگوی Factory Method (کارخانه متد)

## 🎯 هدف
الگوی Factory Method یک الگوی طراحی سازنده است که یک رابط برای ایجاد اشیاء در یک کلاس پایه فراهم می‌کند، اما به زیرکلاس‌ها اجازه می‌دهد که نوع اشیاء ایجاد شده را تغییر دهند.

## 🤔 مشکل
تصور کنید در حال توسعه یک برنامه مدیریت حمل‌ونقل هستید. نسخه اولیه شما فقط حمل‌ونقل جاده‌ای (کامیون) را مدیریت می‌کند. اما حالا می‌خواهید حمل‌ونقل دریایی (کشتی) را هم اضافه کنید. 

مشکل اینجاست که اکثر کدهای شما به کلاس `Truck` وابسته است. اضافه کردن `Ship` به برنامه نیاز به تغییرات گسترده در کل پایگاه کد دارد. اگر بخواهید بعداً حمل‌ونقل هوایی هم اضافه کنید، احتماالً باید دوباره تمام این تغییرات را انجام دهید.

## 💡 راه‌حل
الگوی Factory Method پیشنهاد می‌کند که ایجاد مستقیم اشیاء (با استفاده از عملگر `new`) را با فراخوانی یک متد کارخانه خاص جایگزین کنید. اشیاء همچنان از طریق عملگر `new` ایجاد می‌شوند، اما از داخل متد کارخانه فراخوانی می‌شوند.

## 🏗️ ساختار

```
                    ┌─────────────────┐
                    │    Creator      │
                    ├─────────────────┤
                    │ + factoryMethod()│──────┐
                    │ + someOperation()│      │ creates
                    └─────────────────┘      │
                            △                │
                            │                │
                 ┌──────────┴─────────┐      │
                 │                    │      │
        ┌────────────────┐   ┌────────────────┐
        │ConcreteCreatorA│   │ConcreteCreatorB│
        ├────────────────┤   ├────────────────┤
        │+factoryMethod()│   │+factoryMethod()│
        └────────────────┘   └────────────────┘
                 │                    │
                 │ returns            │ returns
                 ↓                    ↓
        ┌────────────────┐   ┌────────────────┐
        │ConcreteProductA│   │ConcreteProductB│
        └────────────────┘   └────────────────┘
                       ↑         ↑
                       └────┬────┘
                            │
                     ┌──────────────┐
                     │   Product    │ (Interface)
                     └──────────────┘
```

## 👥 شرکت‌کنندگان

1. **Product**: رابط محصولاتی که متد کارخانه ایجاد می‌کند
2. **ConcreteProduct**: پیاده‌سازی‌های مختلف رابط Product
3. **Creator**: کلاسی که متد کارخانه را اعلام می‌کند
4. **ConcreteCreator**: متد کارخانه را بازنویسی می‌کند تا نمونه‌ای از ConcreteProduct را برگرداند

## 🔄 نحوه همکاری
- Creator به زیرکلاس‌های خود متکی است تا متد کارخانه را پیاده‌سازی کنند و بنابراین نمونه مناسبی از ConcreteProduct را برگردانند.

## ⚖️ پیامدها

### مزایا ✅
- **جداسازی کد ایجاد محصول**: می‌توانید کد ایجاد محصول را در یک مکان جمع کنید
- **اصل باز/بسته (Open/Closed)**: می‌توانید انواع جدیدی از محصولات را بدون شکستن کد موجود معرفی کنید
- **اصل تک مسئولیتی (Single Responsibility)**: مسئولیت ایجاد محصول را در یک مکان متمرکز می‌کنید

### معایب ❌
- کد ممکن است پیچیده‌تر شود زیرا نیاز است زیرکلاس‌های زیادی برای پیاده‌سازی الگو معرفی کنید

## 💻 مثال کد (Python)

```python
from abc import ABC, abstractmethod

# Product Interface
class Transport(ABC):
    @abstractmethod
    def deliver(self) -> str:
        pass

# Concrete Products
class Truck(Transport):
    def deliver(self) -> str:
        return "🚚 حمل و نقل از طریق جاده با کامیون"

class Ship(Transport):
    def deliver(self) -> str:
        return "🚢 حمل و نقل از طریق دریا با کشتی"

class Plane(Transport):
    def deliver(self) -> str:
        return "✈️ حمل و نقل از طریق هوا با هواپیما"

# Creator
class Logistics(ABC):
    @abstractmethod
    def create_transport(self) -> Transport:
        """متد کارخانه - باید توسط زیرکلاس‌ها پیاده‌سازی شود"""
        pass
    
    def plan_delivery(self) -> str:
        """عملیات اصلی که از متد کارخانه استفاده می‌کند"""
        transport = self.create_transport()
        result = f"📦 لجستیک: برنامه‌ریزی تحویل...\n{transport.deliver()}"
        return result

# Concrete Creators
class RoadLogistics(Logistics):
    def create_transport(self) -> Transport:
        return Truck()

class SeaLogistics(Logistics):
    def create_transport(self) -> Transport:
        return Ship()

class AirLogistics(Logistics):
    def create_transport(self) -> Transport:
        return Plane()

# استفاده از الگو
def client_code(logistics: Logistics):
    print(logistics.plan_delivery())
    print("-" * 50)

if __name__ == "__main__":
    print("🏭 الگوی Factory Method در عمل\n")
    
    print("📍 لجستیک جاده‌ای:")
    client_code(RoadLogistics())
    
    print("📍 لجستیک دریایی:")
    client_code(SeaLogistics())
    
    print("📍 لجستیک هوایی:")
    client_code(AirLogistics())
```

## 🎯 مثال کاربردی واقعی

### مثال 1: سیستم صدور گزارش
```python
class Report(ABC):
    @abstractmethod
    def generate(self) -> str:
        pass

class PDFReport(Report):
    def generate(self) -> str:
        return "📄 گزارش PDF ایجاد شد"

class ExcelReport(Report):
    def generate(self) -> str:
        return "📊 گزارش Excel ایجاد شد"

class HTMLReport(Report):
    def generate(self) -> str:
        return "🌐 گزارش HTML ایجاد شد"

class ReportGenerator(ABC):
    @abstractmethod
    def create_report(self) -> Report:
        pass
    
    def export(self) -> str:
        report = self.create_report()
        return report.generate()

class PDFReportGenerator(ReportGenerator):
    def create_report(self) -> Report:
        return PDFReport()

class ExcelReportGenerator(ReportGenerator):
    def create_report(self) -> Report:
        return ExcelReport()
```

### مثال 2: سیستم پرداخت
```python
class PaymentMethod(ABC):
    @abstractmethod
    def process_payment(self, amount: float) -> str:
        pass

class CreditCardPayment(PaymentMethod):
    def process_payment(self, amount: float) -> str:
        return f"💳 پرداخت {amount} تومان با کارت اعتباری"

class PayPalPayment(PaymentMethod):
    def process_payment(self, amount: float) -> str:
        return f"🌐 پرداخت {amount} تومان با PayPal"

class CryptoPayment(PaymentMethod):
    def process_payment(self, amount: float) -> str:
        return f"₿ پرداخت {amount} تومان با ارز دیجیتال"
```

## 🔍 چه زمانی استفاده کنیم؟

1. **زمانی که نمی‌دانید از قبل با چه نوع دقیقی از اشیاء سروکار دارید**
2. **زمانی که می‌خواهید کاربران کتابخانه/فریمورک شما بتوانند اجزای داخلی را گسترش دهند**
3. **زمانی که می‌خواهید منابع سیستم را با استفاده مجدد از اشیاء موجود ذخیره کنید**

## 📚 ارتباط با الگوهای دیگر

- **Abstract Factory** معمولاً بر اساس مجموعه‌ای از Factory Methods پیاده‌سازی می‌شود
- **Template Method** معمولاً از Factory Methods استفاده می‌کند
- **Prototype** نیاز به وراثت ندارد، اما نیاز به عملیات Initialize پیچیده دارد. Factory Method بر اساس وراثت است اما نیاز به مرحله Initialize ندارد

## 🎓 نکات پیاده‌سازی

1. همه محصولات باید همان رابط را دنبال کنند
2. متد کارخانه در کلاس پایه می‌تواند پیاده‌سازی پیش‌فرض داشته باشد
3. از نامگذاری واضح استفاده کنید: `createXXX`, `buildXXX`, `makeXXX`
4. در نظر بگیرید از پارامترها برای کنترل نوع محصول استفاده کنید

---

> **یادآوری**: Factory Method به شما کمک می‌کند کد انعطاف‌پذیرتری بنویسید که به راحتی قابل گسترش است! 🚀
